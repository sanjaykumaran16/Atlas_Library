<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="/css/styles_mem.css">
</head>

<body>
    <header id="welcome-header" class="bg-light p-3">
        
    </header>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/member">Status</a>
        <a class="nav-link" href="/bookrecommendation">Book Recommendations</a>
        <a class="nav-link" href="/bookSearch">Search for Books</a>
        <a class="nav-link" href="/borrowed">Borrow History</a>
        <a class="nav-link" href="../login">Logout</a>
    </nav>

    <section id="dashboard" class="container mt-4">
        <h2 class="mb-4">Status</h2>
        <p class="lead">This is your dashboard section. You can view your activities and quick stats here.</p>
        <div class="alert alert-success" style="font-size: 1.2em; font-weight: bold; padding: 15px; border-radius: 10px;">
            Your balance: <%= balance %>
        </div>
        <div class="mt-3">
            <table>
               
                <tbody>
                    <tr>
                        <th>Favorite Genre</th>
                        <td><%= favoriteGenre %></td>
                    </tr>
                    
                    <tr>
                        <th>Books Read</th>
                        <td><%= booksRead %></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </section>
    <style>
        table {
            width: 90%;
            border-collapse: collapse;
            margin: 20px auto;
            background: #fff;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
            border-radius: 10px;
            overflow: hidden;
        }

        th,td {
            padding: 16px;
            text-align: left;
            border-bottom: 1px solid #eee;
        }

        th {
            background-color: #4e54c8;
            color: white;
        }

        tr:hover {
            background-color: #f1f1f1;
        }
</style>
</body>
<script>
    // Fetch user info from the server
    fetch('/user-info')
        .then(response => {
            if (!response.ok) {
                throw new Error('Failed to fetch user info');
            }
            return response.json();
        })
        .then(data => {
            // Update the header with the user's name
            const header = document.getElementById('welcome-header');
            header.textContent = `Welcome, ${data.username}!`;
        })
        .catch(error => {
            console.error('Error fetching user info:', error);
        });
</script>
</html>
